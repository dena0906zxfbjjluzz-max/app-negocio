# Copia a secrets.toml (local) o pega en Streamlit Cloud → Settings → Secrets.
# NUNCA subas secrets.toml a GitHub.

[credenciales]
usuario = "admin"
clave = "cambia-esta-clave-segura"

# Opcional (Supabase más adelante)
# SUPABASE_URL = "https://xxxx.supabase.co"
# SUPABASE_KEY = "tu-clave"
